# Habit: add-todo

**When:** a session surfaces an open design decision, a deferred item, or a PENDING dependency that should be tracked.
**Action:** add a dated entry `- [ ] [YYYY-MM-DD] <item>` to the `## TODO` section of the relevant lens file. Group under a sub-header if the lens already organizes TODOs by topic.
